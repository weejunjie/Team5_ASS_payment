name: Deploy to AWS EC2

on:
  push:
    branches:
      - 'deploy/*'

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mysql-test:
        image: mysql:latest
        env:
          MYSQL_ROOT_PASSWORD: root123 # Set the root password
          MYSQL_USER: user123 # Optional: Create a non-root user
          MYSQL_PASSWORD: password123 # Password for the non-root user
          MYSQL_DATABASE: mysqldb
        ports:
          - "3306:3306"
        options:
          --health-cmd="mysqladmin ping --host=mysql-test --user=root --password=root123"
          --health-timeout=5s
          --health-retries=10
          --health-start-period=30s
        entrypoint: >
          /bin/bash -c "docker-entrypoint.sh mysqld --init-command='ALTER USER \"root\"@\"localhost\" IDENTIFIED WITH mysql_native_password BY \"root123\";'"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java & Build Spring Boot App
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Get MySQL container IP address
        run: |
          CONTAINER_ID=$(docker ps -q --filter "ancestor=mysql:latest")
          CONTAINER_IP=$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' $CONTAINER_ID)
          echo "MySQL container IP is $CONTAINER_IP"
          echo "CONTAINER_IP=$CONTAINER_IP" >> $GITHUB_ENV

      - name: Wait for MySQL to be ready
        run: |
          CONTAINER_ID=$(docker ps -q --filter "ancestor=mysql:latest")
          echo "Waiting for MySQL to be ready..."
          until mysqladmin ping --host=$CONTAINER_IP --port=3306 --user=root --password=root123 --silent; do
            echo "Waiting for MySQL..."
            sleep 5
          done
          echo "MySQL is ready!"
          DB_IP="$CONTAINER_IP:3306"
          echo "DB_IP=$DB_IP" >> $GITHUB_ENV
          echo "MySQL container IP is $DB_IP"

      - name: Build JAR Unit Test
        env:
          DB_IP: ${{ env.DB_IP }}
          DB_DB: mysqldb
          DB_USERNAME: root
          DB_PASSWORD: password123
        run: |
          echo "MySQL container IP is $DB_IP"
          mvn clean verify -Ddb.ip=$DB_IP -Ddb.db=$DB_DB -Ddb.username=$DB_USERNAME -Ddb.password=$DB_PASSWORD
#
#  deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up Java & Build Spring Boot App
#        uses: actions/setup-java@v3
#        with:
#          distribution: 'temurin'
#          java-version: '17'
#
#      - name: Build JAR Unit Test
#        run: mvn clean verify
#
#      - name: Configure AWS CLI
#        if: ${{ success() }}
#        uses: aws-actions/configure-aws-credentials@v1
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ap-southeast-1
#
#      - name: Push Docker image to ECR
#        if: ${{ success() }}
#        run: |
#          aws ecr get-login-password --region ap-southeast-1 | docker login --username AWS --password-stdin 409151997352.dkr.ecr.ap-southeast-1.amazonaws.com
#
#      - name: Build and Tag Docker Image
#        if: ${{ success() }}
#        run: |
#          docker build -t payment_microservice -f Dockerfile.payment .
#
#      - name: Tag Docker image
#        if: ${{ success() }}
#        run: |
#          docker tag payment_microservice:latest 409151997352.dkr.ecr.ap-southeast-1.amazonaws.com/ass-backend:payment_microservice-latest
#
#      - name: Push Docker image to ECR
#        if: ${{ success() }}
#        run: |
#          docker push 409151997352.dkr.ecr.ap-southeast-1.amazonaws.com/ass-backend:payment_microservice-latest

#      - name: Create SSH key file
#        if: ${{ success() }}
#        run: |
#          ssh-keygen -t rsa -N "" -f my_key
#          aws ec2-instance-connect send-ssh-public-key --region ap-southeast-2 --availability-zone  ap-southeast-2c --instance-id i-0248cc38c32d5b1a7 --instance-os-user ec2-user --ssh-public-key file://my_key.pub
#          ssh-keygen -t rsa -N "" -f my_key_test
#          aws ec2-instance-connect send-ssh-public-key --region ap-southeast-2 --availability-zone  ap-southeast-2b --instance-id i-0cf28d0737d84322d --instance-os-user ec2-user --ssh-public-key file://my_key_test.pub
#
#      - name: SSH into EC2 instance and pull docker image to actual server
#        if: ${{ success() }}
#        run: |
#          ssh -o StrictHostKeyChecking=no -i my_key ec2-user@ec2-13-210-101-186.ap-southeast-2.compute.amazonaws.com << EOF
#          aws ecr get-login-password --region ap-southeast-2 | docker login --username AWS --password-stdin 409151997352.dkr.ecr.ap-southeast-2.amazonaws.com
#          docker image prune -f
#          docker pull 409151997352.dkr.ecr.ap-southeast-2.amazonaws.com/payment_microservice
#          docker rm -f payment_microservice || true && docker run -d -e AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }} -e AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }} -e EC2=true --name payment_microservice -p 8080:8080 409151997352.dkr.ecr.ap-southeast-2.amazonaws.com/payment_microservice
#          EOF
#
#      - name: SSH into EC2 instance and pull docker image to test server
#        if: ${{ success() }}
#        run: |
#          ssh -o StrictHostKeyChecking=no -i my_key_test ec2-user@ec2-54-252-168-241.ap-southeast-2.compute.amazonaws.com << EOF
#          aws ecr get-login-password --region ap-southeast-2 | docker login --username AWS --password-stdin 409151997352.dkr.ecr.ap-southeast-2.amazonaws.com
#          docker image prune -f
#          docker pull 409151997352.dkr.ecr.ap-southeast-2.amazonaws.com/payment_microservice
#          docker rm -f payment_microservice_test || true && docker run -d -e AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }} -e AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }} -e EC2_TEST=true --name payment_microservice_test -p 8080:8080 409151997352.dkr.ecr.ap-southeast-2.amazonaws.com/payment_microservice
#          EOF
#
#      - name: Cleanup
#        run: docker logout